{
  "id": "PythonScript",
  "groups" : {
    "position": [ "group1" ],
    "group1": {
       "position" : ["script"],
       "fields" : {
          "script" : {
             "widget": "python-editor",
             "properties" : {
                "default" : "def transform(input, context):\n  return input\n"
             }
          }
       }
    }
  },
  "schema" : {
     "widget": "schema",
     "schema-types" : [ "boolean", "int", "long", "float", "double", "bytes", "string", "map<string, string>" ],
     "schema-default-type" : "string"
  }
}
